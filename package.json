{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "cross-env AWS_ACCESS_KEY_ID=fake AWS_SECRET_ACCESS_KEY=fake jest",
    "deploy": "serverless deploy --stage production",
    "deploy-dev": "serverless deploy --stage dev"
  },
  "dependencies": {
    "express": "^4.17.1",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "axios": "^1.2.1",
    "cross-env": "^7.0.3",
    "jest": "^29.3.1",
    "jest-dev-server": "^6.2.0",
    "serverless": "^3.25.1",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-offline": "^12.0.2"
  }
}
